version: "3.9"

services:
  api:
    build: .
    container_name: api-reliability-access-control
    ports:
      - "8001:8000"
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
    restart: always

  postgres:
    image: postgres:15
    container_name: api-reliability-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: access_control
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  redis:
    image: redis:7
    container_name: api-reliability-redis
    ports:
      - "6380:6379"
    restart: always

volumes:
  postgres_data: